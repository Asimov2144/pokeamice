# Vim
*~
*.sw[p_]

# IntelliJ IDEA
*.idea

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
.bundle-cache
.bundle-plugin
.bundle-user-config
.gem-spec-cache
.ruby-tmp
Gemfile.lock
**/vendor/bundle

# Node.js and NPM
node_modules
npm-debug.log*
package-lock.json
codekit-config.json

# Local source archives and large media dumps
weibo
DREAM 2008.12
archive/raw-images
# Game Freak archive binaries are local preservation material. Keep the
# reproducible manifests, normalized content, translations, and reports in Git.
archive/gamefreak-director/raw/
archive/gamefreak-director/assets/original/
archive/gamefreak-director/warc/
archive/gamefreak-director/reports/first-run-obsolete-assets/
# LINE BLOG raw HTML and source-resolution working copies are reproducible
# from their manifests. The published local image copies remain versioned.
archive/gamefreak-lineblog/raw/
archive/gamefreak-lineblog/assets/
migration/inbox
migration/import-wizard

# Local OCR experiments
.venv-*
.venv-ocr
.venv-ocr-gpu
.venv-mineru
.ocr-home
.paddlex-cache
ocr-output
ocr-output-*

# macOS
.DS_Store

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_site
jekyll_output.log

# Local OCR experiment output and scratch scans (reproducible, not site content)
ocr-ab-tests
DREAM */
test/dream-*
test/scantailor-*
test/*.jpg
migration/*.log

# Automation test runs are local artifacts; keep only their write-ups
automation-tests/**
!automation-tests/
!automation-tests/*.md

# Python bytecode
__pycache__/
*.py[cod]

# Prepared scan output; regenerate with tools/prepare_scan_pages.py
scan-prepared/
.preview/

# Local Jekyll build used to check pages before pushing.
.preview-build/
.claude/launch.json
/vendor/

# Editorial progress for the workflow board; shared between the board and
# anything driving the pipeline through /api/workflow-state, machine-local.
.workflow-state/
