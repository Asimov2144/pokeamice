source "https://rubygems.org"

# 指定 Ruby 版本
# ruby "~> 3.3.6"  # 建议使用兼容性更好的 3.0.x

# 强制使用纯 Ruby 版本的 gem（如果不需要原生扩展）

gem "bigdecimal", "~> 3.1.9", :platforms => [:ruby]
gem "json", "~> 2.9.1", :platforms => [:ruby, :x64_mingw]

# 其他依赖项
# gem "minimal-mistakes-jekyll", path: "."
# 排除 Windows 平台的特殊处理
gem "eventmachine", "~> 1.2.7", :platforms => [:ruby, :x64_mingw]
# gem "jekyll-archives", "~> 2.3.0", :platforms => [:ruby]
gemspec
